# ANKI_CLOZE.md — Anki Cloze 작성 기준

사용자가 Anki Cloze 문제를 요청하면 이 파일을 먼저 확인한다.

---

## 작성 기준

- Cloze 문제는 문장을 외우게 하는 용도가 아니라 핵심 개념 연결을 떠올리게 하는 용도로 만든다.
- 빈칸은 `명사`, `역할`, `명령어`, `상태값`처럼 짧고 핵심적인 단위로 만든다.
- 긴 설명 문장은 가급적 그대로 노출하고, 핵심 단어만 블랭크 처리한다.
- 한 카드에 cloze는 보통 1~2개 정도로 제한한다.
- "왜 그런가"를 묻는 내용은 긴 cloze로 만들지 말고 별도 카드로 분리한다.
- 문제와 설명은 서로 다른 코드블록으로 나눈다.
- 코드블록 안에는 `문제:` 또는 `설명:` 같은 라벨을 넣지 않는다.
- 문제 블록의 첫 줄에는 복습할 때 맥락을 알 수 있도록 주제나 상황을 짧게 적는다.
- 문제 블록에는 필요한 앞맥락을 짧게 넣고, cloze 문장은 다음 줄에 둔다.
- 모바일 Anki에서 읽기 쉽도록 문제 문장은 의미 단위로 짧게 개행한다.

---

## 좋은 방향

```text
Redis Stream에서

{{c1::Producer}}가 메시지를 추가하면
Stream에 기록되고,

{{c2::Consumer}}는 필요한 위치부터
메시지를 읽을 수 있다.
```

```text
Redis Stream은 Redis 안에 저장되는 메시지 로그다.
읽은 메시지가 바로 삭제되지 않기 때문에 장애 발생 시 추적하기 좋다.
```

---

## 피할 방향

```text
Redis Stream은 Redis 안에 저장되는 {{c1::메시지 로그}}다. Producer가 메시지를 추가하면 Stream에 기록되고, Consumer는 {{c2::필요한 위치부터 메시지를 읽을 수 있다}}.
```

- `필요한 위치부터 메시지를 읽을 수 있다`처럼 긴 블랭크는 복습할 때 떠올리기 어렵다.
- `Producer`, `Consumer`처럼 역할 자체를 묻는 쪽이 더 명확하다.
